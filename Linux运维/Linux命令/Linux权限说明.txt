1、chmod 777 filename

2、sudo chmod -R 777可以使文件的权限都改为可读可写可执行。



3、770表示其它用户没有任何权限。



4、771表示其它用户只有可执行权限。



5、772表示其它用户只有可写权限。



6、774表示其它用户只有可读权限。



7、773表示其它用户有可写可执行权限。

 

8、775表示其它用户可读可执行权限。



9、776表示其它用户可读可写权限。